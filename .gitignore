# Local testing instance — never committed (see CLAUDE.md File Registry)
data/database.duckdb

# Python artifacts
__pycache__/
*.pyc
.venv/
.pytest_cache/
.ruff_cache/

# botasaurus/ScraperFC artifacts
downloaded_files/

# Isolated soccerdata install (nightly flywheel only; never the runner env)
.soccerdata-venv/

# Local data exports (generated on demand; not part of the committed dataset)
exports/
