# Offline model-training dependencies (scripts/train_emotion.py only).
# Deliberately NOT in requirements.txt or requirements-dev.txt: scikit-learn is
# never imported at runtime or in CI tests - the trained models are exported to
# pure-numpy artifacts in data/models/ and scored by src/textmodel.py. Install
# this only to reproduce training: pip install -r requirements-train.txt
-r requirements.txt
scikit-learn==1.7.0
